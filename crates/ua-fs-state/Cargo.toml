[package]
name = "ua-fs-state"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Filesystem-backed agent state checkpointing"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
