[package]
name = "ua-memory"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Cross-session agent memory for unified-agent-rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
async-trait = "0.1"
