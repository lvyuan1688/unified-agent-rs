[package]
name = "ua-event-bus"
version = "0.1.8"
edition = "2021"
license = "MIT"
description = "In-memory event bus for unified-agent-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
